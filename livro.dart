class Livro {
  // Atributos privados
  String? _nome;
  int? _paginas;

  // Construtor
  Livro(this._nome, this._paginas);

  // Getters
  String? get nome => _nome;
  int? get paginas => _paginas;

}
